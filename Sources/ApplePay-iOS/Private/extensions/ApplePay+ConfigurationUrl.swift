import Foundation
import SharedDataModels_iOS

// NOTE: URL-based config helpers have been removed.
// Locale, theme, and public key are now read directly from the config dict
// stored on ApplePayButton.currentConfig.
